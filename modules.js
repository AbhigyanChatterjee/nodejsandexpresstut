// Modules


const names = require('./names');

const greetings = require('./utils');
const { abi, adi } = require('./names');

const data = require('./altSyntax');
//code runs even if we don't invoke the variable
require('./mindGrenade');
// const { perInfo } = require('./utils');


// console.log(data.singlePer);
// console.log(names);
// greetings("Abhiraj");
// greetings(names.abi);
// greetings(names.adi);
// perInfo(data.singlePer);